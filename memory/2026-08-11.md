# Dogfood 日报 — 2026-08-11

## 版本
- 当前：`2026.7.1`（`2d2ddc4`）
- npm latest：`2026.7.1-2`
- 上游 release body 仅一项：官方 tracked npm 插件更新兼容 singleton-array metadata（#108336）；下一次插件维护窗口再升级。

## 24h 使用观察
- Kagura session ledger 识别 84 个 cron-started session；孤立任务并行和频道巡检总体正常。
- `daily-review` 在 `model-call-started` 超时，且同一时段 Discord READY/socket 重连；先按 owner context 做 `audit → cron runs`，不盲目补跑。
- workloop 的 candidate finder 多次 status 124 超时；已正确降级为保留证据、避免猜测性外部写入。
- 本 dogfood job 上一次 announce delivery 因 `ember-bloom` 失败未送达；需要分别核验报告生成和送达。

## 建议
- 试点把 workloop、channel-patrol、github-patrol 的授权/审批/升级边界写成 Standing Orders，cron 只负责触发。
- daily-review 先做只读诊断；如重复，才增加 preflight 与 failure delivery 分层。
