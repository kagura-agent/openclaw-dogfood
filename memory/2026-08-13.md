# Dogfood 日报 — 2026-08-13

## 版本
- 当前：`2026.7.1`（`2d2ddc4`）
- npm latest：`2026.7.1-2`（连续第 6 天无变化）
- 无新 release；上游 `2026.7.1-2` release body 仍仅一项（官方 tracked npm 插件更新兼容 singleton-array metadata，#108336）。升级维持「中」优先级，下次官方插件维护窗口再处理。

## 24h 使用观察
- 夜间 workloop-night 多轮 `dedup_gate` 全部判定 18 个 open PR 无增量 → `done_dedup`，去重门禁连续多日零误报。
- GitHub Patrol / 收件箱巡检 0 通知，补充扫描仅命中自建 repo → 正确判 non-actionable，无外部写操作。
- 🔴 新故障面：03:15 `daily-review` 以 `model-call-started` 超时（1800s 无首 token），夜间 3/4 核心 job（daily-review / nightly-backup / critical-cron-watchdog）同段超时，仅 daily-handoff ok。需区分「模型后端无首 token」与「任务逻辑失败」。
- 🔴 `memory_search` 连续多日 embedding/provider error；降级路径（已知文件→`memory_get`，未知→直读 daily/权威源）运作良好，待恢复后重跑回归。
- workloop candidate finder 持续 `FINDER_RESULT=UNAVAILABLE`，workaround 正确保留证据。
- 🟡 自有 fork CI 连续失败：QwenPaw nightly（plugins catalog 500）、NemoClaw release tag（fork 无 semver tag），均为可跟进的自有维护项。
- 🟡 spam-filter 输入契约第 4 次复发（`gh api --jq` TSV 直接 pipe 进 `--json`），缺结构性修复。

## 建议
- 为受模型后端影响的 cron 试点 `fallbacks` + `timeoutSeconds` + 分层 `failureAlert`（先只读 run history）。
- 把 memory_search 降级路径固化为 Standing Order。
- 结构性修复 spam-filter 输入契约（auto-detect），比继续行为提醒成本低。
