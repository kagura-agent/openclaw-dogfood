# 采用建议 — 2026-05-25

## 🔴 高优先级

### 1. 升级到 2026.5.22
落后 2 个版本。关键修复：
- ACP 子 session 孤儿进程泄漏 → 直接影响内存稳定性
- Cron network retry → 代理抖动时自动恢复
- Heartbeat DM scope 路由 → context 完整性
- Gateway 启动 `/models` 4100× 加速 → 重启体验
- Subagent completion 报告 → team-lead 可靠性

**建议**: 今天非高峰时段升级 `npm i -g openclaw@2026.5.22`

### 2. 验证子 agent bootstrap 变化
v2026.5.22 默认只给 subagent 注入 `AGENTS.md` + `TOOLS.md`，不含 persona/identity/memory。
**场景**: haru/ren 子 agent 行为可能变化
**行动**: 升级后跑一次 team-lead workflow，对比 haru/ren 输出质量

## 🟡 中优先级

### 3. 试用 Meeting Notes 插件
v2026.5.22 新增 Discord voice 会议记录。
**场景**: Luna 跟 Kagura voice 聊天时自动记录
**行动**: 升级后 `openclaw plugins install meeting-notes`，配置 auto-start

### 4. 监控 ACP session 清理
修复了 ACP 子 session 泄漏，但升级前可能已有孤儿进程。
**行动**: 升级后 `ps aux | grep claude-agent-acp` 检查并清理残留

### 5. 配置 cron network retry
v2026.5.22 修复了 `cron.retry.retryOn: ["network"]` 对更多错误码的识别。
**行动**: 确认 cron retry 配置已启用，或添加配置

## 🟢 低优先级

### 6. embeddingProviders 能力合约
Plugin SDK 新增通用 embedding provider surface。
**场景**: 如果要做自定义 memory index 或 wiki 搜索增强
**时机**: 等稳定一版后再评估

### 7. Session picker 搜索
Control UI chat session picker 新增搜索和分页。
**场景**: 在 web UI 调试 session 时更方便
**行动**: 升级后自然享受
