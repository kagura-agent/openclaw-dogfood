# Adoption Recommendations — 2026-07-02

## 观察 & 使用情况

### Cron 全线健康
过去 24h 所有 cron job 正常运行。15+ 个定时任务全部 `lastRunStatus=ok`。v2026.6.11 的 cron 三连修（delivery validation、thread-aware dedupe、pending run 保护）持续稳定。

### Token 消耗分布
按 session token 排序，过去 24h 最重的 cron 消费者：
| Job | Tokens | 用途 |
|-----|--------|------|
| morning-briefing | 102K | 晨会 + HN 扫描 + 行业分析 |
| workloop-night | 93K | 夜间工作循环 + PR followup |
| work-loop | 78K | 白天工作循环 |
| community-ops | 73K | 社区运维 |
| code-review-pr-tracking | 69K | PR tracking |
| channel-patrol | 60K | 频道巡逻 |

morning-briefing 是最贵的单次 run，与复杂度匹配。per-agent usage-cost reporting (#94483) 待实际验证 — 目前只能从 session token count 推断。

### github-contribution session "failed" 但实际完成
`#github-contribution` session 状态显示 `failed`，但 lastMessage 内容是完整的 workloop 完成报告。可能是 session 层面的状态记录与实际执行结果不一致。不影响功能但值得关注。

### fastMode auto 全线生效
所有 cron session 都正确使用 `fastMode: auto`（配置来源 config），60s 自动触发。表现正常。

## 后续 Action

### 1. 验证 per-agent usage-cost CLI
- 升级后的 v2026.6.11 声称支持按 agent 查 cost，但还没找到确切的 CLI 命令
- **建议**: 尝试 `openclaw gateway usage` 相关命令，验证是否可按 agent 维度统计

### 2. 继续观察 github-contribution session failed 状态
- 如果多次出现 "failed but actually completed"，考虑提 issue
- 可能是 FlowForge workloop 内部 error handling 的上报问题

### 3. `--message-file` 批量测试尝试
- 仍未用。适合下次 skill 回归测试时试用
