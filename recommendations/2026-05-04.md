# Adoption Recommendations - 2026-05-04

## Immediate (safe to adopt now)

### 1. Upgrade to 2026.5.2
- **Why**: heartbeat scheduler fix alone is worth it — our `Asia/Shanghai` timezone was likely causing wasted quiet-hours ticks
- **How**: `npm i -g openclaw@2026.5.2 && openclaw gateway restart`
- **Risk**: Low — run `openclaw doctor --fix` after to migrate threadBindings and plugin configs

### 2. Run `openclaw doctor --fix` post-upgrade
- **Why**: auto-migrates `threadBindings.spawnSessions`, stale plugin manifests, and configured-plugin installs
- **Benefit**: cleaner config, unified thread spawn toggle

### 3. Adopt `heartbeat_respond` tool
- **Why**: structured heartbeat outcomes instead of parsing `HEARTBEAT_OK` from text
- **How**: no config needed — tool is available automatically for heartbeat runs
- **Benefit**: more reliable heartbeat quiet-pass detection

### 4. Run `openclaw proxy validate`
- **Why**: verify our proxy setup (127.0.0.1:1083) is correctly routing
- **How**: `openclaw proxy validate`
- **Benefit**: catch any proxy misconfig before it causes silent failures

## Short-term (test this week)

### 5. Enable `skipOptionalBootstrapFiles` for subagents
- **Where**: `agents.defaults.skipOptionalBootstrapFiles`
- **Why**: faster subagent spawns, less noise in isolated tasks
- **Test**: measure spawn time difference

### 6. Configure Discord `accessGroup` for allowlists
- **Why**: consolidate per-channel allowlist into reusable groups
- **Where**: new `accessGroup:<name>` syntax in channel auth config
