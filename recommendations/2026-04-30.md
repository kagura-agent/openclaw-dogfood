# Adoption Recommendations — 2026-04-30

## Priority: Upgrade to 2026.4.27

### Immediate Adoption

1. **Upgrade OpenClaw to 2026.4.27**
   - Dream Diary timeout fix (15s → 60s) — directly improves our 12 nightly sessions
   - Dream fan-out cap — prevents unbounded subagent spawning
   - Memory flush no longer leaks into transcripts — cleaner session exports
   - `sessions_yield` fix — more reliable subagent results
   - Gateway startup doesn't block on model prewarm — faster Discord reconnects

2. **Migrate to first-class proxy config**
   ```yaml
   proxy:
     enabled: true
     proxyUrl: "http://127.0.0.1:1083"
   ```
   Replace all `https_proxy` env var usage in cron jobs and scripts. Proper lifecycle management (cleanup on exit, loopback bypass).

3. **Consider `models.pricing.enabled: false`**
   If we don't need OpenRouter/LiteLLM pricing data, disabling skips startup fetches and reduces boot time.

### Near-Term (This Week)

4. **DeepInfra provider for embeddings**
   Now bundled with model discovery and embeddings support. Could complement Ollama for embedding workloads if we hit local resource limits.

5. **Cron `delivery.threadId`**
   If we add Telegram forum topic delivery for any cron job, this is now properly supported in the schema.

### Tracking

6. **Sandbox GPU passthrough** — When agent-tamagotchi or other projects need GPU inside sandbox
7. **Codex Computer Use** — When we want desktop automation through Codex path
8. **Plugin SDK host hooks** — When we build custom plugins for our workflow
