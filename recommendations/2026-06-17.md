# 采用建议 — 2026-06-17

## 昨日建议跟进

### 🟡 给关键 cron 配置 failureAlert — 仍未采用
- 今日 #cove session 正常，昨天的 failed 未复现
- 建议仍有效：failureAlert 能在 session 异常时快速通知
- 优先级：中（从高降低，因为问题未复现）

### 🟡 community-ops token 使用偏高 — 连续第二天观察
- 今日 59k tokens 输出 HEARTBEAT_OK
- 需要 lightContext 或 session rotation
- 优先级：中

### 🟢 channel-patrol seeds → 故事素材 — 持续观察
- 今日 channel-patrol NO_REPLY，未产出 seeds
- 机制本身正常，等下次有素材时观察

### ✅ Vega gemini-2.5-pro — 数据继续收集
- 当前 198 rounds, 90% reliability, 5% unique rate
- 结论趋于明确：reliability 改善但 unique rate 不行
- 可能方案：换 gemini-3.1-pro、调 prompt、或接受当前定位（diversity backup）

## 新建议

### 🔴 升级到 v2026.6.8（高优先级）
- 167 PRs 大版本，多项直接影响修复：
  - heartbeat 去重 → 减少 token 浪费
  - memory_search transient mode 修复 → cron isolated session 更可靠
  - subagent abort 恢复 → 防止 restart 时丢失 subagent
  - session identity in prompt → 减少手动传递
- **建议**: 选择低活跃时段（下午 14:00-15:00）执行升级
- **风险**: 大版本跨度，建议升级后监控 1-2 个 cron 周期

### 🟡 给 abti-loop 加 rate limit 前置检测
- 当前浪费 44k tokens 只为发现 rate limited
- **建议**: 在 cron payload message 中加入 "先检查 rate limit 状态，如果 limited 则直接 skip 并输出预计恢复时间"
- 或者调整 abti-loop 触发时间避开 rate limit 窗口

### 🟡 采用原生 /usage footer（v2026.6.8 新功能）
- 升级后可启用 built-in usage footer
- 每次 cron delivery 自动附带 token 消耗信息
- 替代我们手动在 usage/ 文件中追踪 token 的方式
- **前置**: 先完成升级

### 🟡 利用 session identity 简化 channel-patrol prompt
- v2026.6.8 在 runtime prompt 中暴露 session identity
- channel-patrol 等多 channel 任务不再需要在 payload 中重复说明"你在哪个 channel"
- **前置**: 先完成升级，验证 prompt 中确实包含 identity 信息
