# Adoption Recommendations - 2026-05-07

## 1. Upgrade to v2026.5.6

**Priority: MEDIUM — do after today's cron cycle**

Key benefits:
- `web_fetch` timeout cleanup (our crons depend on this)
- Discord heartbeat ACK fix (may resolve intermittent reconnect churn)
- xAI reasoning effort fix (unblocks `grok-4.3` usage)
- Plugin fetch header fix (prevents silent plugin HTTP failures)

```bash
export https_proxy=http://127.0.0.1:1083
openclaw update
```

## 2. Enable Discord Reasoning Progress Drafts

**Priority: LOW — cosmetic improvement**

v2026.5.5 shows live reasoning text in Discord progress drafts. Currently we see bare `Reasoning` status lines. This is automatic after upgrade — no config needed.

## 3. Add Uptime Check to toolchain-health

**Priority: LOW — operational hygiene**

v2026.5.5 adds Gateway/host uptime to `/status`. Our toolchain-health cron could parse this for restart detection:
- Track `gateway uptime` across checks
- Alert if uptime resets unexpectedly (indicates crash/restart)

## 4. Consider `toolProgressDetail: "raw"` for Debug

**Priority: LOW — debugging aid**

New config option `agents.defaults.toolProgressDetail: "raw"` shows full command output in progress drafts. Not for production (noisy), but could be toggled on when debugging specific cron issues.

## 5. Review `models auth list` for Auth Audit

**Priority: LOW — security hygiene**

New CLI command: `openclaw models auth list [--provider <id>] [--json]`
Could add to periodic toolchain-health checks to verify auth profiles are correctly configured without exposing secrets.
