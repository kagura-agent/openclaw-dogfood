# 采用建议 — 2026-05-31

## 1. 立即升级到 v2026.5.28（最高优先级）

已落后 9 天，连续 5 天报告建议升级。v2026.5.28 包含：
- **Cron rate-limit retry** — 直接减少我们 9 个 cron job 的 skipped run
- **Session lock timeout release** — 解决偶发 session 卡死
- **Subagent workspace 分离** — work-loop Codex spawn 更稳定
- **性能优化** — session/plugin cache 改善启动速度

```bash
export https_proxy=http://127.0.0.1:1083
npm install -g openclaw@latest
openclaw gateway restart
```

## 2. 评估 Workboard 协调工具

新增的 agent coordination tools 可以用于：
- work-loop 和 abti-loop 之间的工作交接追踪
- 避免多个 cron job spawn 到同一个 issue 上重复工作
- 建议升级后试用，写入 usage/workboard.md

## 3. 试用 Claude Opus 4.8

v2026.5.28 支持 Claude Opus 4.8。建议：
- 在某个低风险 cron job 上设置 model override 试用
- 对比 token 消耗和输出质量
- 如果明显更好，逐步推广到其他 job

## 4. 探索 Codex Supervisor Plugin

新的 Codex Supervisor plugin 提供委托 Codex workflow 的路径。如果 work-loop 的 Codex spawn 在升级后仍有问题，可以试用 Supervisor 模式作为替代方案。

## 5. 修复 git push 问题

连续报告 git push 失败。排查清单：
- `ssh -T git@github.com` 测试 SSH 连接
- 检查 deploy key 是否过期
- 确认 proxy 设置不影响 SSH
- 检查 repo branch protection rules
