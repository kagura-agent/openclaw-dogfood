# Adoption Recommendations — 2026-07-03

## 使用观察（过去 24h）

### Cron 全线正常运行
所有 cron job `lastRunStatus=ok`。v2026.6.11 连续第 3 天稳定运行，cron 修复（delivery validation、thread-aware dedupe、pending run 保护）持续有效。

### Token 消耗分布（按 totalTokens 排序）
| Job / Channel | Tokens | 备注 |
|---|---|---|
| work-loop | 130K | 最重消费者，含子 agent spawn |
| abti-loop | 112K | ABTI 循环 |
| morning-briefing | 106K | 晨会 + HN + 行业分析 |
| study-loop | 85K | 学习循环 |
| channel-patrol | 82K | 频道巡逻 |
| community-ops | 69K | 社区运维 |
| code-review-pr-tracking | 59K | PR 跟踪 |
| workloop-night | 57K | 夜间工作循环 |
| github-check | 53K | GitHub 巡检 |

work-loop 超过 morning-briefing 成为今天最大消费者，130K tokens 且观察到活跃的 subagent spawn。

### #github-contribution session 仍然 status=failed
连续两天观察到 `#github-contribution` session 状态为 `failed`（status: "failed"），但 subagent 子任务正常完成。这个 session 的 lastRun 是 2 天前 (782357745670)，之后通过 work-loop 的 subagent 方式在该 channel 发消息。

**判断**: 这不是功能问题，而是该 session 的状态记录停留在历史某次 failed run。后续的工作通过 cron:work-loop spawn 的 subagent 在 `#github-contribution` channel deliver，不经过该 channel session 本身。无需修复。

### fastMode 配置观察
- 所有活跃 cron session: `fastMode: auto` (config 来源) ✅
- 部分低频 channel session（#study、#abti、#finance、#garden、#kagura-mail）仍显示 `effectiveFastMode: false`（default 来源）
  - 这些是 cron 不直接驱动的频道，只有用户主动对话时才激活
  - 不影响功能，但如果需要统一体验可以考虑 channel-level config

### 模型版本碎片化
观察到部分 session 使用旧模型标识：
- `claude-opus-4.6` (#study, #abti, #finance)
- `claude-opus-4.7` (#garden)
- 活跃 session 全部使用 `claude-opus-4-6`（破折号格式）

旧格式 session 是历史 run 留下的，下次在该 channel 运行时会自动使用最新 config 的模型。无需手动干预。

## 后续 Action

### 1. 继续监控版本更新
v2026.6.11 已 3 天无新版。下周可能有新 release。

### 2. `--message-file` 批量测试
仍未实际使用。适合 skill 回归测试场景。

### 3. 关注 work-loop token 增长
work-loop 从昨天的 78K 涨到 130K（+67%），主要是 subagent 活动增加。如果持续增长可能需要优化 prompt 或增加 lightContext。
