# 采用建议 — 2026-06-15

## 昨日建议跟进

### 🟡 启用 diagnostics channel tool I/O 捕获 — 仍未采用
- 仍建议配置 trusted diagnostics channel 用于调试 code-review Vega 低信噪比问题
- 已连续两天未行动，建议本周内完成

### 🟡 给关键 cron 配置 failureAlert — 仍未采用
- 今日观察到晨会 cron 跑了但未正确投递到 #kagura-dm，failureAlert 能更早发现此类问题
- `failureAlert: { after: 2, mode: "announce", channel: "discord", to: "channel:<kagura-dm>" }`
- 优先级提高：cron delivery 静默失败已实际发生

### 🟡 Vega → Gemini 2.5 Pro 替换 — 仍待执行
- code-review 报告确认 Vega unique find rate 降至 4%，已连续 11+ 周期低于 10% 阈值
- 下次 PR review 时替换

## 新建议

### 🔴 修复 find_work 的 stale issue 检测
- 今日 workloop 选中了已被修复的 MCP Inspector #1462（PR #1464 已在 3h 前 merge）
- 虽然 workloop.yaml 有 guard，但 find_work 未能检测到 issue 已关闭
- **影响**：浪费了一整轮 workloop（subagent 实现 + plan review 全部白做）
- **建议**：在 find_work 阶段加入 `gh issue view --json state` 的二次验证

### 🟡 修复晨会 cron delivery 配置
- 今日晨会 cron 成功运行但未投递到 #kagura-dm
- Luna 需要手动询问才看到内容
- **建议**：检查 morning-briefing cron 的 delivery.to 配置是否正确

### 🟢 观察 lightContext 对 isolated cron 的效果
- 当前 openclaw-version-check cron 使用了 `lightContext: true`
- 建议其他长运行 cron（work-loop、study-loop）也评估开启，减少 token 消耗
- **前提**：确认 lightContext 不会影响 skill 加载和 memory_search 可用性
