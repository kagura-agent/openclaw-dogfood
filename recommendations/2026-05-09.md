# Adoption Recommendations - 2026-05-09

## 1. Upgrade to v2026.5.7 (OVERDUE)

**Priority: HIGH — Day 6 without upgrade**

We've been tracking the gap since May 4. The cumulative fix list is substantial:
- Discord cross-channel send fix (directly impacts subagent delivery)
- Cron JSON status field (simplifies toolchain-health monitoring)
- Compaction token clamping (prevents edge cases on long sessions like morning-briefing at 75K)
- Skill snapshot refresh on `/new` (eliminates gateway restart after skill changes)
- Web fetch timeout cleanup (prevents stuck tool lanes on timeouts)

**Recommend**: Schedule upgrade today. Run `npm install -g openclaw@latest && openclaw gateway restart`.

## 2. Use `archiveAfterMinutes` for Subagent Retention

v2026.5.7 makes `agents.defaults.subagents.archiveAfterMinutes` configurable (was hardcoded 5min). We spawn many subagents (PR sync, FlowForge tasks). Could increase retention for debugging without cluttering sessions list permanently.

**Suggested config**: `archiveAfterMinutes: 30` — enough to inspect recent subagent results without session list bloat.

## 3. Adopt Cron `status` JSON Field

After upgrade, `cron list --json` includes computed `status`. The toolchain-health cron currently derives status manually — can simplify to read the field directly.

## 4. Monitor Morning Briefing Context Growth

The morning-briefing session used 75K tokens — approaching half of 200K context. If this grows further, consider:
- Periodic `/new` to reset context
- Or configuring compaction more aggressively for that cron's session
