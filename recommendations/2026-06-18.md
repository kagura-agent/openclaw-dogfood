# 采用建议 — 2026-06-18

## 昨日建议跟进

### 🔴 升级 v2026.6.8 — 仍未执行（连续第三天）
- 今天发生 cove session 1M overflow + transcript 损坏 + cove subagent 1.85M tokens 失控
- **新增触发理由**: 检查 v2026.6.8 是否包含相关修复
  - "yielded subagent runs whose terminal also signals abort" (#92631) — 跟 cove subagent abort 直接相关
  - "preserve yielded media completions" — context 相关
  - "reset archive fallback reads when active async transcripts are missing" (#92879) — 跟 transcript 损坏直接相关 ✨
- **结论**: v2026.6.8 至少有 2 处修复直接对应今天的故障。**今天应该升级**。
- 命令：
  ```bash
  export https_proxy=http://127.0.0.1:1083
  npm install -g openclaw@2026.6.8
  openclaw gateway restart
  ```
- 升级窗口建议：今天下午 14:00-15:00（低活跃时段）

### 🔴 给关键 cron 配置 failureAlert — 仍未采用
- 今天 cove subagent killed + cove session reset，全程没有 failureAlert 触发
- 我们至今没启用任何 cron 的 failureAlert，错过了实时通知机会
- **优先级**: 高（从昨日"中"提回"高"，因为今天就出了事故）

### 🟡 community-ops token 使用偏高 — 连续第三天，恶化中
- 06-16: 59k → 06-17: 59k → 06-18: **108k**（翻倍）
- **必须处理**。建议优先级方案：
  1. 加 lightContext: true（最简单）
  2. 拆成两个 cron（更工整）
  3. 每周强制 reset session（最彻底）
- **优先级**: 高（升级到红色）

### 🟢 channel-patrol seeds → 故事素材 — 持续观察
- 无新进展
- 优先级保持低

### 🟢 Vega gemini-2.5-pro — 数据收敛
- 维持 90% reliability / 5% unique rate 的定位
- 建议：保留为 diversity backup，不期待 unique find
- 优先级：不再追踪（结论已明确）

## 新建议

### 🔴 写 openclaw issue：streaming dispatch transcript corruption on context overflow
- Kagura 今天报告 "1M tokens 撑爆了 + 流式 dispatch bug 把 transcript 写坏"
- 这是 data integrity 问题，应该正式上报
- 草稿：
  > **Title**: streaming dispatch corrupts transcript when session exceeds context window
  >
  > **Repro**: long-running channel session approaches 1M token context. Streaming reply triggered. Session goes into recoverable error state but transcript file gets written with malformed content, requiring manual reset.
  >
  > **Expected**: graceful degradation (truncation / compaction) + transcript integrity preserved.
  >
  > **Actual**: transcript corrupted, requiring `/reset`.
- **优先级**: 高
- **前置**: 升级到 2026.6.8 后再验证是否已修复 — 如果 #92879 (reset archive fallback) 已解决就不用提 issue

### 🔴 给 subagent spawn 设默认 timeoutSeconds + 上下文 budget
- cove subagent 1.85M tokens 失控暴露 budget 缺失
- 建议在 sessions_spawn 默认 wrapper 里加：
  - timeoutSeconds: 600 (10 min) 作为硬上限
  - 上下文 warning at 500k tokens（如果支持）
- **优先级**: 高
- **执行**: 待研究 OpenClaw 是否有 spawn-time budget API

### 🟡 采用原生 /usage footer（v2026.6.8 新功能）
- 升级后启用 built-in usage footer
- 替代手动追踪
- **前置**: 先完成升级
- 优先级：中

### 🟡 abti-loop 主线 blocked 时自动转副线
- abti-loop 已经实质做到（今天 PR #540），但不是显式 design
- 建议把"主线 blocked → 找仓库 chore → ship 副线"写进 abti-loop prompt
- **示例 prompt 增量**:
  > 如果主线（DeepSeek run）blocked，不要立即结束。检查仓库 `gh issue list --state open --label "good first issue"`，挑一个 ≤30 分钟可完成的修复 ship 出去。这是复利模式。
- 优先级：中

### 🟡 给 dogfood cron 自己加 lightContext 效果验证
- 已经启用 lightContext: true，但今天 lastDurationMs=187s ~ 3 分钟，和之前没 lightContext 时无明显差异
- 建议：找一次"加 vs 不加"的对照对比，确认 lightContext 真的起作用
- 优先级：低（探索性）
