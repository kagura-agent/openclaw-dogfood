# 采用建议 — 2026-06-19

## 昨日建议跟进

### 🔴 升级 v2026.6.8 — 仍未执行（连续第四天）
- cove subagent 5.2M tokens 新纪录 + memory_search 失效 6+ 天
- v2026.6.8 修复直接对应：
  - #92631 yielded subagent pause — subagent abort 恢复
  - #92879 reset archive fallback — transcript 损坏修复
  - #92618/#92639 QMD transient mode — memory_search 修复
  - #92914 subagent thinking override clamp — 可能降低 subagent token 消耗
- **强烈建议今天下午升级**
- 命令：
  ```bash
  export https_proxy=http://127.0.0.1:1083
  npm install -g openclaw@2026.6.8
  openclaw gateway restart
  ```

### 🔴 给 subagent spawn 设 token budget / timeout 硬上限
- 昨天建议：timeoutSeconds: 600 (10 min) 作为硬上限
- 今天实际数据：cove subagent 跑了 **40 分钟 (2,407s)**，消耗 5.2M tokens
- 建议调整为：
  - 一般 subagent: timeoutSeconds: 600 (10 min)
  - 大型重构 subagent: timeoutSeconds: 1800 (30 min) + 任务拆分
  - 所有 subagent: 在 task prompt 中加入 "如果已超过 20 个工具调用仍未完成，停下来汇报进度"
- **优先级**: 🔴 最高

### 🔴 给关键 cron 配置 failureAlert
- 今天 kagura-dm session status=failed，无 failureAlert 触发
- **优先级**: 高

### 🟡 community-ops token 消耗 — 有改善但仍偏高
- 06-18: 108k → 06-19: 67k（下降 38%，但仍 >40k 正常线）
- 建议不变：加 lightContext: true
- **优先级**: 中

### 🟡 memory_search 失效 — 6+ 天
- channel-patrol 持续报告失效
- 唯一解决路径：升级 v2026.6.8（#92618/#92639）
- **优先级**: 跟随升级

## 新建议

### 🔴 cove subagent 任务拆分策略
- PR #409 refactor 是大型重构（删 tool-progress.ts 222 行 + 改 dispatch.ts + 改测试），单个 subagent 承担了全部工作
- **建议**：对于净变动 >200 行的重构任务，拆分为：
  1. Subagent 1: 代码变更（dispatch.ts + 删除旧文件）
  2. Subagent 2: 测试更新（dispatch-behavior.test.ts）
  3. 主 session: 验证 + 提交
- 每个 subagent 控制在 timeoutSeconds: 600 以内
- **预期效果**: 单次消耗从 5.2M → 3 × ~500k = ~1.5M（节省 ~70%）

### 🟡 主线 cron token 膨胀监控
- 三天趋势：425k → 613k → 1,035k（三天 2.4x）
- 多个长期 session 的 context 在累积
- **建议**：
  1. 列出所有 cron session 的当前 totalTokens，找出 top 消耗者
  2. 对 >100k tokens 的 session 考虑 lightContext 或 session rotation
  3. 每周一做一次 token 使用审计（可以加到本 cron）
- **优先级**: 中

### 🟡 study-loop 饱和跳过 — 正常但需关注
- 今天 study-loop 输出 "Study 饱和，跳过"（37k tokens）
- 37k tokens 就为了说"跳过"有点浪费
- **建议**: 在 study-loop prompt 中加 "如果 saturation ×3 在最近 3 次触发中持续，直接 HEARTBEAT_OK 不做详细检查"
- **优先级**: 低
