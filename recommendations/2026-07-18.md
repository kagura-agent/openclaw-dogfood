# 采用建议 — 2026-07-18

## 状态更新

### 已完成采用 ✅
- **Session 标题自动生成** — 第 8 天持续正常
- **Cron 正确性大修** — 第 8 天零故障
- **CJK Markdown emphasis** — 持续验证
- **Delivery recovery pacing** — 稳定

### 进行中观察 👀
- **Dreaming sessions timeout** — 连续两个 narrative session 被 killed
  - 需要检查 timeout 配置是否匹配创意写作场景的生成时间

## 🟡 本周可推进

### 1. Exit-triggered Cron Schedules（延续）
- **场景**: work-loop 完成后自动触发 plan-review（目前已通过 subagent 实现）
- **现状**: 独立 cron 定时 + subagent 内联调用
- **改进**: exit-trigger 让编排更声明式
- **行动**: 在 workloop-night → github-check 之间试验
- **优先级**: 中

### 2. Dreaming Session Timeout 调优
- **观察**: 两个 dreaming 叙事 session（rem + light）都被 killed（abortedLastRun: true）
- **可能原因**: timeoutSeconds 设置过短（创意写作可能需要更长生成时间）
- **行动**: 检查 dreaming cron 的 timeout 配置，尝试延长到 120s+
- **优先级**: 中（不影响核心功能但影响创意输出质量）

### 3. Session Model 批量刷新
- **观察**: 5 个 Discord channel session 仍显示旧格式 `claude-opus-4.6`（带点）
- **影响**: 视觉混乱，不确定是否影响模型路由
- **行动**: 通过 API 或 Control UI 批量更新这些 session 的 model 字段
- **优先级**: 低

## 🟢 暂不行动

### 4. ClawRouter 多 Provider
- 单 provider 稳定运行中，无切换需求

### 5. GPT-5.6 作为 Fallback
- 未遇到 rate limit 或故障，暂不需要
