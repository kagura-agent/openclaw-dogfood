# Usage Observations - 2026-05-11

## Cron Activity (past 24h)

Active cron jobs observed:
- **openclaw-version-check** — this job
- **weekly-eval** — running (Sunday weekly evaluation)
- **channel-patrol** — running
- **study-loop** — completed, studied agent-skills-eval repo (skill A/B testing framework)
- **community-ops** — clean patrol, 0 issues/PRs/unread
- **abti-loop** — tested Qwen3-32B (PTCF archetype confirmed), discovered proxy bypass bug (#281)
- **github-check** — quiet, 30 open PRs all healthy
- **work-loop** — followup of 20 open PRs across 11 repos, closed opencode #26606 (upstream fixed), pinged #25654 (7 days no review)
- **kagura-mail** — merged 3 PRs (#91, #84, #82), spawned subagent for duplicate reply fix (PR #93)

## What's Working Well

1. **Subagent spawning for code tasks** — kagura-mail channel spawned a subagent that wrote a complete duplicate-reply dedup feature (DuplicateReplyError, 9 new tests, all 259 passing) and opened PR #93. End-to-end autonomous.
2. **FlowForge study workflow** — study-loop ran a structured quick-scan → deep-read pipeline, identified agent-skills-eval as worth studying, wrote analysis notes
3. **Cross-channel message delivery** — cron reports landing in correct Discord channels consistently
4. **Git operations** — PR merging, branch management, all working autonomously in cron context

## Friction Points

1. **Cron delivery context isolation** — Filed #80468 about cron delivery only reaching UI, not entering session context. Agent has "split brain" — user sees full channel, agent only sees interactive messages. This is a structural issue.
2. **Proxy bypass in CLI** — ABTI loop discovered CLI's `https.request` bypasses configured proxy, filed #281. Affects our proxy-dependent setup.
3. **NemoClaw at PR limit** — 5 open PRs at the cap, all waiting on bot-only reviews (coderabbitai). No human reviewer yet. Needs patience.
4. **hermes-agent CI failures** — 3 PRs stuck on upstream CI issues, documented but not actionable on our end.

## Patterns

- **Weekly eval running on Sunday** — first time seeing this cron in the observation window. Good for periodic self-assessment.
- **PR velocity** — 3 PRs merged in kagura-mail in one session, plus a new feature PR. Autonomous dev loop is productive.
- **Paperclip channel drift** — channel topic says "Paperclip research" but content has drifted. Raised for cleanup decision.
