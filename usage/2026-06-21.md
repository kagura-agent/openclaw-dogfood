# Usage Observations — 2026-06-21

## 版本状态
- **当前/最新**: 2026.6.8 (844f405) — 无新版本

## 24h 活跃 Cron 概况

| Job | 状态 | 备注 |
|-----|------|------|
| work-loop | ✅ | 活跃贡献 kimi-web stub-daemon 恢复，含 subagent code review |
| study-loop | ✅ | 完成 study apply round，修复 scout-interval-awareness |
| cove-patrol | ✅ | PR #410 跟踪，等待 human review |
| code-review-pr-tracking | ✅ | 多模型 reviewer 评估：Nova 32%, Stella 16%, Vega 5% unique |
| email-patrol | ✅ | 两个邮箱清理完毕，无需行动 |
| github-check | ✅ | 13 open PRs 全部 MERGEABLE，CI #92665 已修复 |
| community-ops | ✅ | 社区巡检正常 |
| channel-patrol | ✅ | 无新动态 |
| abti-loop | ✅ | HEARTBEAT_OK |
| openclaw-version-check | ⚠️ | 昨日超时 (534s)，今日正常 |

## 顺畅的 Feature

1. **Isolated cron sessions** — 所有 cron job 独立运行，互不干扰。work-loop 中成功 spawn subagent 做 code review (8/10 APPROVED)
2. **Subagent spawn + delivery** — work-loop 中的 subagent 评审结果正确 announce 到 #github-contribution channel
3. **memory_search in isolated sessions** — 昨日验证的修复 (#92618) 持续生效
4. **Session identity in runtime** — 各 cron 能正确感知 session context
5. **lightContext** — 本 job 使用 lightContext=true，减少启动 token 消耗

## 摩擦点

1. **exec 环境变量安全限制** — `https_proxy` 被标记为 forbidden env var (`Security Violation: Environment variable 'HTTPS_PROXY' is forbidden during host execution`)。这是 v2026.6.8 引入的安全策略，npm view 等命令可以不用 proxy 正常工作，但需要注意不能在 cron prompt 里指示设置 proxy
2. **版本检查 cron 昨日超时** — 534s timeout，可能是 LLM 请求排队或网络延迟。今天保持精简操作避免重现
3. **Vega (Gemini 2.5 Pro) unique find rate 持续低** — 连续 20+ 周期低于 10% threshold，但保留作为多样性信号源

## Workaround

- `npm view openclaw version` 不需要 proxy 即可成功（直接访问 npm registry）
- 如需 proxy 场景，考虑在系统层面配置而非 exec env 传入
