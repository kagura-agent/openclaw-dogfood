# 使用观察 — 2026-08-13

## 过去 24h 的 cron / channel 使用

### 覆盖边界
- 依据 Kagura session ledger（active 1440min）+ 当日 daily memory（`memory/2026-08-13.md`）交叉核验。夜间 workloop-night、GitHub Patrol、daily-review、server-health、study、morning-briefing、memory-eval、channel-patrol 等 cron 均按周期运行并留下可审计记录。

## 顺畅的功能 ✅
- **夜间去重门禁稳定。**4+ 轮 workloop-night 的 `dedup_gate` 全部判定 18 个 open PR 的 `updatedAt` 快照与 `/tmp/workloop-night-state.txt` 一致 → `done_dedup`，未重复跟进；不为「有产出」而制造动作，去重逻辑连续多日零误报。
- **巡检静默边界有效。**GitHub Patrol 与收件箱巡检 0 通知，补充扫描仅命中自建 repo（cove#529 / finance / wiki），无外部互动 → 正确判定 non-actionable，未执行任何外部写操作或 ping。
- **学习飞轮门控准确。**study followup/quick-scan/scout 三轮门控（followup-status + study-saturation）正确路由，识别生态稳定期（pi 生态簇 + Docker Sandboxes 均已知），portfolio 超限时选择 prune 而非新增 deep-read。

## 摩擦与 workaround ⚠️
- 🔴 **核心 cron 出现 model-call 超时（新）。**06:00 Daily Audit 发现今日 03:15 `daily-review` 以 `model-call-started` 超时失败（deepseek-v4-pro/floway-sg-deepseek，1800s 无首 token），且夜间 03:00–04:30 四核心 job 中 3 个 timeout（daily-review / nightly-backup / critical-cron-watchdog），仅 daily-handoff ok。这是此前「error-status」之外的一个可复现新故障面，需区分「模型后端无首 token」与「任务逻辑失败」。
- 🔴 **memory_search 连续不可用（多日）。**今日 memory-eval 仍记录 embedding/provider error，检索 3/3 超时。现行降级路径已固定为「已知文件 → `memory_get`，未知 → 直读当日 daily/权威源」，且已建议纳入成功率/P95 监控。此降级路径目前运作良好，但需在恢复后重跑 Janus/Luna 偏好回归（hit@1 与 canonical-hit@5）。
- **workloop candidate finder 持续超时。**tracked scan 仍返回 `FINDER_RESULT=UNAVAILABLE`（exit 2），workaround 保持正确：保留离线证据、明确标记不可用、禁止基于局部结果外部写入。
- 🟡 **自有 fork CI 连续失败（信息性）。**QwenPaw「Full Tests Nightly」连续 3 晚失败（根因 `GET /api/plugins/catalog` 返回 500 → plugin contract 测试失败）；NemoClaw「Label Merged PR Release Target」失败（fork 无 strict semver release tag）。均为自有维护项、非外部贡献，但已进入可跟进的自有 backlog。
- 🟡 **spam-filter 输入契约第 4 次复发。**study scout 再次误用 `gh api --jq` 预格式化 TSV 直接 pipe 进 `spam-filter.sh --json`，输出为空需重跑。gradient 已存在，缺的是结构性修复（让 spam-filter auto-detect 接受 TSV/`--jq` 输入），而非继续行为提醒。

## 配置/环境动态
- Luna 在 #cove 排查 agent run 时间线（已修 404 + abort runId 不匹配，staging 部署中），并暂停 `cove-patrol` / `code-review-pr-tracking` / `spec-review-evolution` 三个 cron。使用观察不应把这三个暂停 cron 的「无产出」误判为故障。

## 下一步
1. 优先按 owner context 读取 `daily-review` / `nightly-backup` / `critical-cron-watchdog` 的 run history，区分「模型后端无首 token 超时」与「任务逻辑失败」；对连续 model-call 超时的 cron 评估配置 model fallback 或 timeoutSeconds。
2. 维持 memory_search 降级路径，恢复后重跑检索回归。
3. 把 QwenPaw / NemoClaw 两条自有 CI 失败纳入可跟进维护项，避免长期静默累积。
