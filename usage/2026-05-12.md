# Usage Observations - 2026-05-12

## Cron System (10+ active jobs)
All cron jobs ran successfully in the last 24h. System is stable.

### What Worked Well
- **Subagent spawning from cron**: `work-loop` successfully spawned a subagent to fix CI on PR #78766 (rebase for lint boundary check failure). Subagent completed in ~8 min with clean results. The spawn→complete→report flow is smooth.
- **FlowForge orchestration**: Both `study-loop` and `workloop-night` used FlowForge workflows effectively. Study loop completed scan + trend analysis; night workloop produced comprehensive 22-PR sync report.
- **Cross-session awareness**: `sessions_list` with `includeLastMessage` provides excellent visibility across cron jobs. Morning briefing can pull context from all other crons.
- **github-check reliability**: Clean patrol, correctly identified claude-hud #537 merge (13th external merge).

### Friction Points
- **HN timeout (study-loop)**: Hacker News connectivity failing for 2+ consecutive days. Study loop notes "要加备用信号源" but no fix implemented. Systemic issue — needs alternative news source.
- **ABTI rate limiting**: `abti-loop` hit rate limits, had to skip cycle. Logged status and will retry. This is expected behavior but limits our testing throughput.
- **work-loop git index corruption**: Had to do `rm .git/index + git reset --hard HEAD` during rebase in openclaw repo. The repo is large and git operations can be fragile. Subagent handled it but it's a recurring pattern.

### Observations
- **daily-audit meta-finding**: Audit itself is stuck in a loop — "observation not closing loop" pattern where 3 behavior issues from yesterday all recurred today. The audit records problems but doesn't execute fixes. This is an OpenClaw usage pattern issue, not a tool issue.
- **Token usage**: Morning briefing consumed ~77k tokens (highest), likely due to context aggregation across all cron sessions. Most cron jobs stay under 40k tokens.
- **email-patrol efficiency**: Quick runs (~21k tokens), correctly auto-archived GitHub notifications.
