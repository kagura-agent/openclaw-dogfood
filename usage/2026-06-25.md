# Usage Record — 2026-06-25

## Adoption 验证 (v2026.6.9 遗留条目)

### ✅ cron compact list (#93395)
- **验证方法**: 直接调用 `cron list` API
- **结果**: 返回 compact 格式，每个 job 仅含 id/name/enabled/nextRunAtMs/scheduleKind/lastRunStatus 六个字段
- **对比**: 之前 list 返回完整 job object (含 payload/schedule/delivery 等大量嵌套)
- **收益**: 在有 30+ job 时查看列表更清晰，减少噪音

### ✅ cron default runMode "due" not "force" (#94453)
- **验证方法**: 分析 dogfood-adoption job 的 21 次历史 run
- **结果**: 仅 1 次 manual 触发 (`runId: "manual:..."`, 2026-06-08)，其余 20 次全部按 schedule 触发
- **证据**: 无意外 force-run 出现。`nextRunAtMs` 间隔一致 (~86400s)
- **收益**: 防止 cron run 命令默认变成 force 导致意外重跑

### ✅ cron isolated setup timeout retry (#94588)  
- **验证方法**: 分析 2026-06-23 的 error → retry → success 序列
- **证据**:
  - Run 1 (18:05:45 CST): LLM network timeout, failed after 58s → `nextRunAtMs` 设为 +2min
  - Run 2 (18:14:50 CST): LLM network timeout, failed after 43s → `nextRunAtMs` 设为 +6min  
  - Run 3 (18:20:40 CST): ✅ 成功完成 (80s, 271K tokens)
- **对比**: 之前 timeout 后直接标 fail 并等下次 24h schedule
- **收益**: 网络瞬断不再导致当天 cron 完全 miss

## 未验证 (升级受阻)

v2026.6.10 的 4 个 adopt 条目因无法在 cron session 中执行升级 (EACCES + elevated disabled) 而未验证。需要人工执行 `sudo npm i -g openclaw@latest && openclaw gateway restart`。
