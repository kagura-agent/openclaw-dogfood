# Usage Observations — 2026-06-26

## Version Status
- **Current**: 2026.6.10 (aa69b12)
- **Latest**: 2026.6.10
- **Status**: ✅ 已是最新

## 24h Cron 运行情况

| Job | Last Status | Notes |
|-----|-------------|-------|
| channel-patrol | ✅ ok | 62K tokens, 正常 patrol + channel summary |
| cove-patrol | ✅ ok | 31K tokens, PR/issue status 报告 |
| study-loop | ✅ ok | 47K tokens, sandboxes followup 完成 |
| community-ops | ✅ ok | 59K tokens, HEARTBEAT_OK |
| auto-trader | ✅ ok | 28K tokens |
| study-daily-summary | ✅ pending | 刚触发 |
| memory-eval | ✅ pending | 刚触发 |
| Memory Dreaming Promotion | ✅ pending | 刚触发 |
| work-loop | ✅ pending | 等待触发 |
| wiki-fix | ✅ pending | 等待触发 |
| openclaw-version-check | 🔄 running | 当前 session |

## 使用观察

### 顺畅的部分

1. **Multi-channel cron 矩阵稳定运行** — 11 个 cron job 全部在线，无 disabled，最近 24h 无 timeout/retry 事件。说明 v2026.6.10 的 cron delivery awareness fix 和 isolated setup timeout retry 在实际运行中表现良好。

2. **Channel patrol 输出质量高** — 09:00 的 patrol 正确聚合了 #cove Luna check-in、#study sandboxes followup、#github-inbox patrol、#kagura-dm 战略晨会、ABTI PR，format 一致。证明 cron agentTurn + delivery announce 的模式已成熟。

3. **Cove patrol PR 跟踪** — 自动检测到 #435 和 #422 的 reviewer assignment、CI status、review comments，输出结构化表格。workflow 端到端无人工干预。

4. **Study loop 智能间隔** — sandboxes 项目增速放缓后自动降级为 monthly revisit (下次 07-10)，体现 follow-up 调度的自适应逻辑。

### 摩擦点

1. **dreaming-narrative-light subagent 失败** — 今天 09:07 spawned，09:09 结束但 status=failed。输出文本已生成（poem + 数字观察），但 session 最终标记为失败。可能是 delivery 或后处理环节出错。需进一步排查 transcript。

2. **Discord channel session 异常终止** — `agent:kagura:discord:channel:1491602968741413039` 显示 status=failed + abortedLastRun=true，runtime 约 10 min。这是一个普通 channel session（Luna 说了 hi），abort 原因不明——可能是 context 过长触发了超时，或者有 concurrent session 冲突。

3. **fastAutoOnSeconds 未体现收益** — 所有 session 的 effectiveFastMode=false。v2026.6.10 引入的 Fast Talks 自动模式（短对话自动启用 fast mode）理论上应该在 channel-patrol 等短 run 上生效，但实际未观察到。可能需要检查 fastAutoOnSeconds 阈值配置是否匹配我们 cron run 的典型时长。

### Workaround

无新 workaround。之前的 "cron session 无法升级" 限制仍在——需要人工 SSH 执行 `npm i -g openclaw@latest`。

## Token 消耗概览

今日已消耗（至 09:26 CST）:
- channel-patrol: 62,855 tokens
- community-ops: 59,196 tokens
- study-loop: 46,947 tokens
- cove-patrol: 31,095 tokens
- auto-trader: 28,498 tokens
- **小计**: ~228K tokens（不含当前 run 及刚触发的 3 个 job）

## 待跟进

- [ ] 排查 dreaming-narrative-light failure 原因
- [ ] 排查 discord channel session abort 原因
- [ ] 验证 Fast Talks 自动模式是否在 cron isolated session 中生效
