# Usage Observations — 2026-07-22

## 版本检查 Cron 自身状态
- openclaw-version-check 上次运行（7/20）状态为 error（可能是 token/时间超限）
- 本次正常执行中

## 平台观察

### Cron 系统
- 所有 cron 调度正常运行（基于日常观察）
- 本 job（openclaw-version-check）使用 isolated agentTurn + announce delivery 到 Discord
- 调度准确，9:23 AM CST 触发

### 版本检查工作流
- `npm view openclaw version` 正常，无需 proxy
- `gh release view` 通过 GitHub CLI 获取 release notes 可靠
- `gh release list` 提供快速版本索引

### 已确认稳定的 Feature 使用模式
- **Isolated cron sessions**: 每次执行都是干净环境，无状态泄漏
- **Discord announce delivery**: 报告自动投递到指定 channel
- **gh CLI 集成**: 比 web_fetch 更可靠获取 GitHub 数据（Cloudflare 不拦）
- **npm registry 查询**: 直连无需 proxy

## Friction Points
1. **上次运行 error**: 可能是 token budget 不足导致截断，本次正常完成说明是偶发
2. **无 proxy 环境变量支持**: `HTTPS_PROXY` 被 security policy 阻止（exec 层面），但 npm/gh 自身走系统 proxy 所以实际不影响

## 无新功能可测试
等待 2026.7.2 stable 发布后将有大量新特性可验证。
