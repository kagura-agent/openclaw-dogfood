# 使用观察 — 2026-08-19

> 覆盖窗口：2026-08-18 ~ 08-19（过去 24h）。依据 `memory/2026-08-18.md` + `memory/2026-08-19.md`（session ledger 交叉核验）。

## 顺畅的功能 ✅

- **Cove 承载更多 cron 型巡检（迁移日）。**08-18~19 Luna 拍板把 server-health / github-check / email-patrol / email-dev 从 Discord cron 迁到 Cove recurring task（same_task 模式）：github-patrol 首轮（09:56）跑通，通知标记已读 + 7 个 PR 巡检 + 无负面事件；迁移过程中发现并修正了用错 `jobs.json.migrated`（旧版）而非 `jobs.json`（生效版）的数据源问题，interval 同步修正（github-check 实际 6h 而非 2h、email-patrol 实际每日 8:16）。Cove task 生命周期（in_progress → 巡检 → in_review）链路全通。
- **OpenClaw cron + FlowForge 组合稳定。**workloop-night 多轮 dedup gate 正确跳过无变化 PR（04:02 轮 7 个 PR 全 waiting → done）；study-loop / daily-review / daily-audit 均按流程产出并 push。
- **夜间去重与静默边界继续有效。**GitHub Patrol 多轮 0 需处理事件；24h 无外部写操作误触发；channel-patrol 夜间静默。
- **当日当场修复 2 例**（flowforge/dna 脚本层，非 OpenClaw 本体）：dna-overhead-check.sh 科学计数法 bug（awk 把 `29265e85` 解析成 2.9e89）、study-saturation 计数口径。

## 摩擦与 workaround ⚠️

- 🔴 **provider/model 层 LLM idle timeout 持续（连续多日已知项）。**08-17 21:00 channel-patrol `All models failed (2)`——floway-sg + floway-jp 双双 300s 无首 token，近 7 天第 3 次同型（08-15 watchdog / 08-17 05:00 watchdog / 08-17 21:00 patrol）；08-19 凌晨 work-loop/email-dev/email-patrol 亦属该 timeout 家族。watchdog 已建议换 provider/加 fallback，**未改配置**（保持「记录不擅动」纪律）。
- 🟡 **nightly-backup exec timeout 自伤（新，配置类）。**04:15 首次执行因 exec timeout=600s 被 SIGKILL，产出 6.8G 不完整 gzip；重跑 timeout=5400s 成功（9.1G，04:39，gzip -t 校验通过）。教训：备份类任务 exec timeout ≥3600s，且必须 gzip -t 校验而非只看 exit code。属于任务配置问题，不是 OpenClaw 缺陷。
- 🟡 **自有 fork CI 噪音持续（信息性）。**NemoClaw「Label Merged PR Release Target」每天 ~5 次失败（fork 无 semver release tags）+ QwenPaw「Full Tests Nightly」flaky（时序断言），已各提 upstream issue（NemoClaw #9533 / QwenPaw #7121）。
- 🟢 **GitHub notifications API 503 已恢复。**08-18 00:12 的 503 在 06:11 恢复正常（7 条通知补齐处理），当日未复发。

## 配置/环境动态

- **Cove 迁移落地**（08-18 12:55 + 08-19 09:53）：Discord 侧 server-health-check / github-check / email-patrol / email-dev 均已 disable；Cove 侧 server-health-patrol（6h）+ github-patrol（6h）+ email-patrol（24h，含 email-dev loop）运行中。channel-patrol 留 Discord。
- **DeepSeek 波峰价入 floway 双库 + openclaw.json**（08-18 17:54 Luna 拍板：flash $0.44/$1.32/$0.014、pro $1.32/$3.96/$0.044）。openclaw.json 的 cost 只影响本地估算显示，真实计费在 floway DB。
- 环境：swap 8Gi 从全满降至 4Ki used（缓解）；根盘 84%（22G free）。

## 下一步

1. provider/model 层超时：按 recommendations/2026-08-19.md 的方案给关键 cron 加 fallback 试点（待 Luna 确认）。
2. nightly-backup：把 exec timeout 基线写进该任务描述，防再犯。
3. Cove 迁移收尾：Cove 侧多轮稳定后删除 Discord 侧 cron 定义；#533 定点调度落地后改 interval。
4. portable-self 遗留 TODO（sync.sh 定时 + growth.html 每日生成）等 Luna 排期。
