# Usage Observations - 2026-05-08

## Cron Health (24h snapshot)

All 10+ cron jobs ran successfully. Active crons observed:
- **version-check** — running now (this job)
- **channel-patrol** — active
- **study-loop** — completed FlowForge study #1564 (mirage project deep read)
- **community-ops** — clean run, no issues
- **finance-daily-us** — delivered successfully
- **abti-loop** — spawned DeepSeek test subagent, hit 429 rate limits (GitHub Models API)
- **email-patrol** — 100 messages scanned, agent token expired/revoked flagged
- **workloop-night** — 24 open PRs tracked, all healthy
- **morning-briefing** — delivered strategic briefing
- **toolchain-health** — clean
- **github-check** — 3 CI failures flagged, notifications processed
- **work-loop** — PR #1408 submitted to jackwener/OpenCLI

## Feature Usage Patterns

### Working Well
- **FlowForge workflow orchestration** — study-loop and work-loop both using FlowForge successfully for multi-step agent tasks
- **Subagent spawning** — abti-loop and github-check both spawn subagents for parallel work; delivery to Discord channels working
- **Cross-channel message delivery** — subagents delivering results to correct Discord channels (though the `discord:channel:` prefix bug in 2026.5.3 may have been silently affecting some sends)
- **GitHub CLI integration** — notification scanning, PR tracking, CI failure detection all smooth
- **Session memory management** — sessions accumulating ~20-70K tokens per cron run, within bounds

### Friction Points
1. **Version gap growing** — 4 versions behind, morning-briefing flagged this as "基建欠债 4 天"
2. **GitHub Models API rate limits** — DeepSeek tests via GitHub Models hit 429 with ~13hr retry-after. Not an OpenClaw issue but affects abti-loop.
3. **Gmail agent token revoked** — email-patrol detected expired token on agent account. Recurring issue.
4. **No `cron show --json` status field** — Our toolchain-health cron can't easily get computed cron status from JSON. Fixed in 2026.5.7, another reason to upgrade.

### Not Yet Using
- Cron JSON status field (available after upgrade to 2026.5.7)
- `channels list --all` (new in 2026.5.7)
- Skill cache refresh on `/new` (new in 2026.5.7)
- `voice.captureSilenceGraceMs` tuning
- `subagents.archiveAfterMinutes` config
