# 使用观察 — 2026-08-02

## Cron 系统状态
- ⚠️ openclaw-version-check — 昨日 (08-01) lastRunStatus="error"
  - lastDeliveryError: "cron: job interrupted by gateway restart"
  - 说明昨天的 cron 运行被 gateway 重启中断
  - 今日 (08-02) 09:48 正常触发运行中

## 系统健康
- **Gateway**: 正常（但昨日有重启事件导致 cron 中断）
- **npm registry**: 通过 proxy 稳定访问
- **gh CLI**: 正常 — release list 和 release view 查询成功
- **exec 并发**: 多命令并行正常

## 观察

### 顺畅的功能
- **gh release view --json**: 成功获取完整 beta.6 changelog（大文本，~15KB），无截断
- **npm info --json**: dist-tags + time 字段完整，可以追踪发布时间线
- **Cron recovery**: 昨日中断后今日正常恢复调度，无需人工干预

### 值得关注的问题
- **Gateway restart 打断 cron**: lastDeliveryError 明确写 "job interrupted by gateway restart"
  - 这正是 v2026.7.2-beta.6 修复的问题之一（durable channel delivery + cron reliability）
  - 升级后此类问题应该消失
  - 当前影响：偶发性日报丢失，需人工补跑或等下次调度

### 新 beta 发布观察
- v2026.7.2-beta.6 于 08-01 05:34 UTC 发布
- npm beta tag 已更新指向 beta.6
- 发布间隔 4 天，节奏恢复正常

## 功能利用率
| 功能 | 今日使用 | 状态 |
|------|---------|------|
| Cron isolated sessions | ✅ | 正常（昨日被重启中断） |
| gh CLI (release view) | ✅ | 大 changelog 获取成功 |
| npm view (dist-tags) | ✅ | 稳定 |
| exec 并发 | ✅ | 稳定 |
| Discord delivery | 待验证 | 本次运行中 |

## 关于 "Automations" 重命名的兼容性提醒

beta.6 将 `cron` tool 重命名为 "Automations"（UI/CLI），但保留 CLI alias。
- 我们的 skill 和文档中大量使用 `cron` 这个名字
- 升级前需要确认：
  1. 工具名是否真的改了（还是只是 UI 标签？）
  2. 如果工具名改了，旧 cron tool 调用是否还兼容
  3. 我们的 cron skill 文档是否需要更新

**Action item**: 升级到 v2026.7.2 时优先测试 cron/Automations 兼容性。
