# Usage Notes — 2026-07-01

## v2026.6.11 Adoption 验证

### ✅ per-agent usage-cost reporting (#94483)
- 命令: `openclaw gateway usage-cost --agent kagura --days 1 --json`
- 结果: 返回了 kagura agent 的 token 使用数据
  - 今日: input=21, output=3678, cacheRead=159467, cacheWrite=59835, totalTokens=223001
  - cache 仍在 refreshing (2304/14888 files cached)，cost 字段暂为 0
- 支持 `--all-agents` 和 `--agent <id>` 过滤
- 后续: 等 cache 完成后重新查看 cost 数据

### ✅ Cron 三连修 (#95754, #95794, #94323)
- **delivery validation (#95754)**: 检查最近 12 次成功 run，全部 `deliveryStatus: "delivered"`，`delivery.intended` 与 `delivery.resolved` 一致，source="explicit"
- **pending runs preserved (#94323)**: 06-23 连续两次网络 error 后，job 正确安排 retry（nextRunAtMs 逐步递增），最终成功执行，未丢失 pending run
- **thread-aware dedupe (#95794)**: 当前无 thread-based cron 场景，标记待观察

### ✅ Long-context tool-result cache-stable (#95624)
- 检查活跃 cron session cache 率:
  - cron:2e87 → 37k tokens, 97% cached
  - cron:2715 → 30k tokens, 94% cached  
  - cron:97dc → 45k tokens, 97% cached
  - discord session → 49k tokens, 99% cached
- 高 token count 下 cache 率稳定在 94-100%，fix 有效
