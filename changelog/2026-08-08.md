# OpenClaw Changelog — 2026-08-08

## 版本状态
- **当前安装版本**：`2026.7.1` (`2d2ddc4`)
- **npm latest**：`2026.7.1-2`
- **状态**：🟡 落后一个 correction release。
- **发布来源**：`gh release view v2026.7.1-2 --repo openclaw/openclaw --json body -q .body`（2026-08-08 查询；未使用 web_fetch）。

## v2026.7.1-2：新特性摘要

> GitHub release body 只发布了 **1 项修复**。以下完整呈现其实际范围，不把上游没有宣布的能力凑成 5–10 项。

### 🆕 对我们有用的新功能（能立即采用的）
- **无。**这是 correction release，没有新功能；不应为不存在的能力改动稳定运行的 cron 或频道策略。

### 🔧 修复了我们遇到过的痛点
1. **受跟踪官方 npm 插件更新兼容新版 npm 的 singleton-array metadata**（#108336）——新版 npm 将单项 metadata 以数组形式返回时，官方插件可以继续安装或升级 correction release；我们依赖 plugin skills，且此前把插件更新兼容性列为维护风险，因此能降低下一次更新被 metadata 格式阻塞的概率。

### ⚡ 值得关注但暂不采用的
1. **修复仅覆盖受跟踪的官方 npm 插件更新路径**——今天没有复现插件安装/更新失败，也没有待更新的官方插件；先不为单一 hotfix 中断 cron，等下一次插件维护窗口或更高稳定版一并升级并回归验证。

## 升级建议
- **优先级：中。**将 `2026.7.1-2` 作为下次官方插件变更前的最低基线。
- **升级后验收**：确认 `openclaw --version`；运行 `openclaw doctor`；进行非破坏性的受跟踪插件查询/更新预演；确认 plugin skill discovery 和一条 Discord cron delivery 正常。
