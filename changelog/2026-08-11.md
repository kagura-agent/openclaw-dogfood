# OpenClaw 版本检查 — 2026-08-11

## 版本
- 已安装：`2026.7.1`（`openclaw --version`）
- npm latest：`2026.7.1-2`（`npm view openclaw version`）
- 状态：🟡 落后一个 correction release。
- 官方来源：`gh release view v2026.7.1-2 --repo openclaw/openclaw --json body -q .body`（2026-08-11 查询；未使用 web_fetch）。

## v2026.7.1-2：新特性摘要

> 官方 release body 只有 **1 项修复**；没有可诚实拆分为 5–10 条的变更。以下完整保留上游范围，避免把不存在的能力包装成新功能。

### 🆕 对我们有用的新功能（能立即采用的）
- **无。**这是 correction release，不公布新增功能；无需改变稳定的频道或 cron 策略。

### 🔧 修复了我们遇到过的痛点
1. **官方受跟踪 npm 插件更新接受新版 npm 客户端的 singleton-array metadata，使插件能安装和更新 correction release（#108336）。**我们运行多项 plugin skills，且已有插件 manifest/schema 摩擦；这降低下一次官方插件维护被 npm metadata 形状阻断的风险。

### ⚡ 值得关注但暂不采用的
1. **修复范围只限受跟踪官方 npm 插件的安装/更新路径。**过去 24 小时没有复现官方插件安装或更新失败，因而不为这个单一 hotfix 打断运行中的 cron fleet；在下一次插件维护窗口合并升级并回归验证。

## 升级建议
- **优先级：中。**将 `2026.7.1-2` 作为下一个官方插件维护窗口的最低升级基线。
- **升级后验收：**确认版本、运行 `openclaw doctor`、做一次非破坏性的 tracked-plugin 查询/更新预演，并验证 plugin skill discovery 与一条 Discord cron delivery。
