# OpenClaw Changelog — 2026-08-05

## 版本状态
- **当前安装版本**: `2026.7.1` (`2d2ddc4`)
- **npm latest**: `2026.7.1-2`
- **状态**: 🟡 有一个 correction release；本机尚未升级。
- **发布来源**: `gh release view v2026.7.1-2 --repo openclaw/openclaw`（2026-08-04 00:41 UTC）

## v2026.7.1-2：实际变更

> 此版本的 GitHub changelog **只有 1 条修复**，没有足够内容可诚实地凑成 5–10 条；以下按影响分类，不重复或虚构变更。

### 🆕 对我们有用的新功能（能立即采用的）
- **无。**这是 correction release，不含新功能；无需为了功能而抢先升级。

### 🔧 修复了我们遇到过的痛点
1. **npm 官方插件更新兼容新版 npm 的 singleton-array metadata**（#108336）——新版 npm 客户端返回单元素数组形式的 metadata 时，OpenClaw 现在可正常安装/更新受跟踪的官方插件到 correction releases；我们近期使用了 channel、搜索与技能类插件，过去也出现过 plugin 更新/兼容问题，因此这是一次低风险的升级可靠性修复。

### ⚡ 值得关注但暂不采用的
- **受影响范围是“tracked official plugins”的安装/更新路径。**当前没有可验证的官方插件更新失败事件，因此先不在生产环境立即升级；下次需安装或更新官方插件时，把 `2026.7.1-2` 作为首选基线并做一次 `openclaw doctor` 验证。

## 升级建议
- **建议：按需升级，优先级中。**当下一次要更新官方插件、或发布更高稳定版时一起升级；单独升级的收益有限，风险也较低。
- **升级后验收**：运行 `openclaw --version`，再执行一次已有官方插件的查询/更新（不改变业务配置），并确认 Discord cron delivery 与 plugin skills discovery 正常。
