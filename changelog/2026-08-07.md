# OpenClaw Changelog — 2026-08-07

## 版本状态
- **当前安装版本**：`2026.7.1` (`2d2ddc4`)
- **npm latest**：`2026.7.1-2`
- **状态**：🟡 落后一个 correction release。
- **发布来源**：`gh release view v2026.7.1-2 --repo openclaw/openclaw --json body -q .body`（2026-08-07 查询）。

## v2026.7.1-2：新特性摘要

> GitHub release body 仅包含 **1 项修复**。为避免重复或虚构，以下按影响分类完整列出，而非凑足 5–10 项。

### 🆕 对我们有用的新功能（能立即采用的）
- **无。**这是 correction release，未发布新增功能；不应为不存在的新能力改变当前稳定的 cron/频道运行方式。

### 🔧 修复了我们遇到过的痛点
1. **npm 官方插件更新现在兼容新版 npm 的 singleton-array metadata**（#108336）——受跟踪官方插件在新版 npm 返回单元素数组 metadata 时可以安装或更新到 correction release；我们依赖插件/skills 且已有插件安装兼容性摩擦记录，因此能降低下次官方插件更新被 metadata 格式卡住的风险。

### ⚡ 值得关注但暂不采用的
- **修复范围只覆盖受跟踪的官方 npm 插件更新。**本轮没有已验证的插件更新失败，且没有需要立刻更新的官方插件；先不为单一 hotfix 中断 cron，待下一次插件变更或更高稳定版一并升级并做回归验证。

## 升级建议
- **优先级：中。**把 `2026.7.1-2` 作为下一次官方插件变更前的最低基线。
- **升级后验收**：确认 `openclaw --version`；运行 `openclaw doctor`；进行非破坏性的受跟踪官方插件查询/更新预演；确认 plugin skill discovery 和一个 Discord cron delivery 正常。
