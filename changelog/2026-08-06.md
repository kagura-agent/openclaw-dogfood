# OpenClaw Changelog — 2026-08-06

## 版本状态
- **当前安装版本**：`2026.7.1` (`2d2ddc4`)
- **npm latest**：`2026.7.1-2`
- **状态**：🟡 当前安装仍落后一个 correction release。
- **发布来源**：`gh release view v2026.7.1-2 --repo openclaw/openclaw --json body -q .body`（发布于 2026-08-04 00:41 UTC）。

## v2026.7.1-2：新特性摘要

> GitHub release changelog 只有 **1 项修复**，没有足够的真实内容可拆成 5–10 项；以下按影响分类完整呈现，避免重复或虚构。

### 🆕 对我们有用的新功能（能立即采用的）
- **无。**这是 correction release，不含功能新增；不需要为新能力紧急变更稳定运行的环境。

### 🔧 修复了我们遇到过的痛点
1. **npm 官方插件更新兼容新版 npm 的 singleton-array metadata**（#108336）——当新版 npm 返回单元素数组 metadata 时，受跟踪官方插件现在可以正常安装和更新到 correction release；我们依赖多种插件/技能，并曾观察到插件安装与兼容性摩擦，因此它降低了下一次插件更新失败的风险。

### ⚡ 值得关注但暂不采用的
- **该修复只覆盖受跟踪官方插件的安装/更新路径。**当前没有已验证的官方插件更新失败事件，单独升级的即时收益有限；待下一次官方插件变更或更高稳定版发布时一并升级，并以插件查询/更新预演和 Discord cron delivery 做回归验证。

## 升级建议
- **建议：按需升级，优先级中。**采用 `2026.7.1-2` 作为下一次插件变更的最低基线，而不是现在为单一 hotfix 打断 cron。
- **升级后验收**：核验 `openclaw --version`；运行 `openclaw doctor`；进行非破坏性的插件查询/更新预演；确认 plugin skills discovery 和 Discord cron delivery 正常。
