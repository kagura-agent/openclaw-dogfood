# OpenClaw Changelog — v2026.5.28 → v2026.6.1

**检测日期**: 2026-06-06
**当前版本**: 2026.5.28 (e932160)
**目标版本**: 2026.6.1 (发布于 2026-06-03)

## 新功能 (feat:)

1. **Skill Workshop** — governed review flow: proposal lists, today actions, revision handoff, searchable file previews, review states, locale coverage, reusable session routing
2. **Workboard 编排原语** — 多 agent 规划和任务追踪 (#87469)
3. **Code mode namespaces** — scoped agent/global sessions (#88043)
4. **Copilot 外部插件** — `@openclaw/copilot` 独立 npm 包
5. **Tokenjuice 外部插件** — `@openclaw/tokenjuice` 独立 npm 包
6. **iPad native layout** — iOS 原生布局
7. **MiniMax M3 模型支持** (#88860)
8. **SecretRef provider integration manifest** (#82326, #88117)
9. **Plugin install index SQLite 持久化** (#88794)
10. **Inbound queues SQLite 存储** + iMessage monitor state SQLite (#88797)
11. **Doctor disk space health checks**
12. **Control UI Dreaming-tab agent selector** (#78748)
13. **Calmer chat composer controls** + first-output latency instrumentation (#88772, #88998)
14. **Core skills index** — 集中化 skills runtime loading/status/filtering

## Bug 修复 (fix:)

1. **Memory 写入串行化 + Linux watcher 自动重连** — serialize QMD update/embed writes, reduce fan-out, retry transient reads, reattach Linux native watchers (#66339, #85931, #89185, #89188, #85351)
2. **Cron rate-limit 自动重试** — retry recurring jobs after transient model rate limits (#cron fix)
3. **Disabled skill SecretRef 隔离** — skip disabled skill env overrides from stale snapshots (#79072, #79173)
4. **Plugin 故障隔离** — one bad plugin does not poison sibling runtime paths (#77237, #88807)
5. **Channel 超时保护** — cap Telegram/Discord/WhatsApp/Signal/Feishu/Teams 等 request/retry timers (#88183)
6. **Agents/CLI 恢复** — interrupted tool calls, stale session bindings, compaction handoffs, media delivery retries (#88129, #88136, #88141, #88162, #88182)
7. **Provider timeout bounds** — bound media downloads, OAuth/device-code lifetimes, local service probes
8. **Cron SQLite migration compatibility** — legacy run-log tables, archived job stores (#88285)
9. **Auth atomic writes** + force re-login recovery + compact before oversized turns (#89181)
10. **CLI: agents add 不再依赖 provider catalog** (#76284, #88314)
11. **Chat UI: Gateway failures 显示为可见 assistant messages**
12. **Streaming tool-call argument parsing** — keep record-shaped when providers emit null/arrays

## Breaking Changes

无明确 breaking changes。

## 配置变更

- Plugin install index 迁移到 SQLite
- iMessage/inbound queues 迁移到 SQLite
- Cron SQLite migration 兼容性更新
