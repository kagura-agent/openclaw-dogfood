# OpenClaw 版本检查 — 2026-08-12

## 版本
- 已安装：`2026.7.1`（`openclaw --version`）
- npm latest：`2026.7.1-2`（`npm view openclaw version`）
- 状态：🟡 落后一个 correction release。
- 官方来源：`gh release view v2026.7.1-2 --repo openclaw/openclaw --json body -q .body`（2026-08-12 查询；未使用 web_fetch）。

## v2026.7.1-2：新特性摘要

> 官方 release body 仅列出 **1 项修复**，没有可诚实拆分为 5–10 条的变更。以下完整保留上游范围，避免将不存在的功能包装成新能力。

### 🆕 对我们有用的新功能（能立即采用的）
- **无。**这是 correction release，不公布新增功能；现有 Discord channel 与 cron 工作流无需因它改变。

### 🔧 修复了我们遇到过的痛点
1. **受跟踪的官方 npm 插件更新现在接受新版 npm 客户端返回的 singleton-array metadata，允许插件安装及更新到 correction release（#108336）。**我们依赖多项 plugin skills，且此前记录过插件 manifest/schema surface 摩擦；此修复可避免后续官方插件维护因 metadata 形状不兼容而被卡住。

### ⚡ 值得关注但暂不采用的
1. **修复只覆盖受跟踪官方 npm 插件的安装/更新路径。**过去 24 小时没有复现该路径故障，故不为一个单点 hotfix 打断稳定的 cron fleet；在下一次插件维护窗口受控升级并验证。

## 升级建议
- **优先级：中。**将 `2026.7.1-2` 作为下个官方插件维护窗口的最低升级基线。
- **升级后验收：**确认版本，运行 `openclaw doctor`，做一次非破坏性的 tracked-plugin 查询/更新预演，并验证 plugin skill discovery 和一条 Discord cron delivery。
