# OpenClaw Changelog — 2026-08-09

## 版本状态
- **当前安装版本**：`2026.7.1` (`2d2ddc4`)
- **npm latest**：`2026.7.1-2`
- **状态**：🟡 落后一个 correction release。
- **发布来源**：`gh release view v2026.7.1-2 --repo openclaw/openclaw --json body -q .body`（2026-08-09 查询；未使用 web_fetch）。

## v2026.7.1-2：新特性摘要

> 上游 release body 只有 **1 项修复**，没有可诚实拆分为 5–10 条的变更；以下保留完整范围，避免把不存在的能力包装成新功能。

### 🆕 对我们有用的新功能（能立即采用的）
- **无。**这是 correction release，不新增功能；不应为不存在的能力改变已稳定运行的频道或 cron 策略。

### 🔧 修复了我们遇到过的痛点
1. **受跟踪官方 npm 插件更新兼容新版 npm 的 singleton-array metadata**（#108336）——新 npm 客户端将单项 metadata 返回为数组时，官方插件仍可安装和更新 correction release；我们依赖 plugin skills，这能降低下次插件维护被 metadata 格式阻断的风险。

### ⚡ 值得关注但暂不采用的
1. **修复范围限于受跟踪的官方 npm 插件更新路径**——过去 24 小时没有复现插件安装/升级失败，也没有待更新的官方插件；暂不为单一 hotfix 打断 cron，等下次插件维护窗口或更高稳定版一并升级、回归验证。

## 升级建议
- **优先级：中。**在下一次官方插件维护前，将 `2026.7.1-2` 作为最低版本基线。
- **升级后验收**：确认 `openclaw --version`、运行 `openclaw doctor`、进行非破坏性的 tracked-plugin 查询/更新预演，并确认 plugin skill discovery 与一条 Discord cron delivery 正常。
