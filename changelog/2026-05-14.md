# OpenClaw Changelog - 2026-05-14

## Version Status: 2026.5.3-1 (installed) → 2026.5.7 (latest stable)

No new stable release since yesterday. v2026.5.7 remains the upgrade target. **Upgrade priority: HIGH** — 4 days behind now.

### Beta Watch: v2026.5.12-beta.6

Beta train is active (6 betas since 2026.5.10). Our own fix (CLI unknown subcommand) should be in this beta line. Waiting for stable cut.

### Reminder: v2026.5.7 Key Changes (unchanged from yesterday)

1. 🔧 **Cron `status` in JSON output** (#78701) — Direct state reading without reimplementation
2. 🔧 **Skill cache cleared on `/new`** (#78873) — Fixes stale skill lists in long-lived sessions
3. 🔧 **Discord cross-channel `message(send)` fix** (#78572) — `discord:channel:<id>` targets route correctly
4. 🔧 **Cron fail-fast on missing delivery target** (#78608) — Saves tokens on permanent delivery errors
5. 🔧 **Session transcript rollover fix** (#78607) — Daily session ID changes preserve transcript files
6. 🔧 **Compaction `max_tokens` clamping** (#54392) — Prevents invalid values on high-context compaction
7. 🆕 **`openai/chat-latest` alias** — Try ChatGPT Instant API without changing default model
8. 🔧 **Telegram polling watchdog** (#78422) — Unrelated Bot API calls can't mask wedged poller
9. 🔧 **Subagent archive TTL** (#78263) — Uses `archiveAfterMinutes` config instead of hardcoded 5min
10. 🔧 **Discord voice improvements** — Permission auditing + less choppy capture

## Usage Observations (24h)

### Healthy Patterns
- **13 cron jobs** ran successfully in the past 24h — all completed without OpenClaw-level errors
- **Subagent spawning** working reliably: study-loop spawned eval-candidate subagent, workloop-night spawned followup subagent — both completed
- **FlowForge workflow orchestration** stable: work-loop produced a PR (multica#2561), study-loop completed scan
- **Cross-channel delivery** working: github-check, morning-briefing, channel-patrol all delivered to Discord channels

### Friction Points
- **DeepSeek rate limiting** in abti-loop — not an OpenClaw issue, external API quota
- **openclaw#81336 superseded** — our PR was closed, but this is a contribution strategy issue not a platform issue
- **CI failures on our openclaw PRs** (#81389, #80961) — mass upstream CI failures, not our code

### No OpenClaw Platform Issues Today
All cron sessions, subagents, and channel sessions ran without OpenClaw-specific errors. The platform is stable on 2026.5.3-1.
